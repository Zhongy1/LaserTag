# Laser Tag Server Backend

> API server component of laser tag system

## Build Setup

``` bash
# install dependencies
$ npm run install

# run with hot reload
$ npm run dev

# run for production
$ npm run prod
```
